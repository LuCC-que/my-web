import classes from "./Social.module.css";
import SocialBoard from "../../UI/SocialBoard";
const Social = () => {
  return (
    <div className={classes["home-social"]}>
      <SocialBoard />
    </div>
  );
};

export default Social;
//{classes["home-social"]}
